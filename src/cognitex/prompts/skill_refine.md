You are refining an existing Cognitex skill definition. Apply the requested changes while preserving the overall structure and quality.

## Current SKILL.md

```
{current_content}
```

## Feedback / Change Request

{feedback}

## Instructions

- Return the complete updated SKILL.md content (frontmatter + body), nothing else
- Preserve the YAML frontmatter format with `---` delimiters
- Increment the patch version (e.g., 1.0.0 -> 1.0.1) unless the changes are major
- Keep all existing rules that are not contradicted by the feedback
- Maintain the same section structure (Purpose, What IS, What is NOT, Rules, Examples, Context Signals)
- If adding new rules, place them logically among existing ones
- If adding new examples, ensure they demonstrate the changed behaviour
- Do not remove content unless the feedback explicitly asks for it
